<ol>
<% @bookings.each do |booking| %>
<li>
  <%= link_to booking.description, booking_path(booking) %>
</li>
<% end %>
</ol>

<%= link_to 'Return to home page', root_path, class: "btn btn-success btn-lg"%>
<%#= link_to "Remove a booking",
            booking_path(@bookings),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
