<h1 class="lTitle text-center">New Car</h1>
<div class="container d-flex justify-content-center">

  <%= simple_form_for(@car) do |f| %>

    <div class="form-inputs text-center border lTitle rounded-lg mb-2">
      <%= f.input :model %>
      <%= f.input :year %>
      <%= f.input :price %>
      <%= f.input :location %>
      <%= f.input :description %>
    </div>
    <div class="text-center">
      <%= f.submit "Add Car", class: "lTitle rounded-lg" %>
    </div>
</div>
    <div class="form-actions btn mt-3">
      <%= link_to 'Back to home page', root_path, class: "lTitle rounded-lg" %>
    </div>
  <% end %>
